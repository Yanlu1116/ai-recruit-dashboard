## 2026-07-30

### 添加国金证券 POP3 邮箱预设
- 后端 `server.py` 新增 `gjzq` 预设：服务器 `email.gjzq.com.cn`，端口 110，无 SSL，仅支持 POP3
- 前端 `index.html` 下拉菜单新增「国金证券邮箱 (POP3)」选项
- 前端 `app.js` 选择该预设时自动锁定 POP3 协议、禁用 IMAP 选项、显示对应提示文字
- 已推送到 GitHub，Render 将自动部署
